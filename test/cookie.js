
/*function getCookie(cookieKey) {
	var x = document.cookie;
	var indexBegin = x.indexOf(cookieKey);
	var cookieContent = undefined;
	if(indexBegin != -1) {
		var indexEnd = x.indexOf(";", indexBegin);
		if (indexEnd == -1) {
			indexEnd = x.length;
		}
		cookieContent = x.substring(indexBegin, indexEnd);
	}
	return cookieContent;
}
	
function setCookie(cookieKey, cookieValue) {
	var input = cookieKey + "=" + cookieValue + ";expires=Fri, 31 Dec 9999 23:59:59 GMT; path=/";
	document.cookie = input;
}*/

